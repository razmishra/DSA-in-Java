class day31 {

    public static void main(String args[]) {
        // int[] arr = { 3, 5, 7, 9, 1, 2, 7, 6, 8 };

        // int n = arr.length;

        // int Sn = (n * (n + 1)) / 2;
        // int S2n = (n * (n + 1) * (2 * n + 1)) / 6;

        // long S = 0;
        // long S2 = 0;
        // for (int i = 0; i < n; i++) {
        //     S += arr[i];
        //     S2 += arr[i] * arr[i];
        // }

        // long val1 = S - Sn;
        // long val2 = S2 - S2n;
        // long val3 = val2 / val1;

        // // find X and Y
        // long X = (val3 + val1) / 2;
        // long Y = X - val1;
        // System.out.println(X);
        // System.out.println(Y);

// ---------------- stringbuffer and stringbuilder --------------------
        // StringBuffer sb = new StringBuffer("john");
        // sb = sb.append(" there");
        // sb = sb.deleteCharAt(0);
        // sb = sb.delete(0,2);
        // String sb1 = sb.toString();
        // sb = sb.insert(0, "hello ");
        // sb = sb.reverse();
        // sb1 = sb.substring(0,3);
        // sb.ensureCapacity(100);
        // System.out.println(sb);

        


    }

}